// Reference to vite/client removed to avoid "Cannot find type definition file" error.
// The 'process' variable is already declared in the environment (e.g. via @types/node), causing a conflict.
// We remove the manual declaration here to fix "Cannot redeclare block-scoped variable 'process'".

export {};
